# Context

## Project

- Name: `mlable`
- Focus: PyTorch utilities for layers, model blocks, metrics, losses, schedulers, and shaping helpers.
- Packaging: generic project (`pyproject.toml`) targeting Python `>=3.10,<3.14`.

## Contributor Workflow

1. Read `docs/index.md` for the repository layout.
2. Read `docs/invariants.md` before making structural changes.
3. Add or update tests under `tests/` for every behaviour change.
4. Record strategic choices in `docs/decisions.md`.
5. Update `docs/roadmap.md` when planning future steps.
